spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/milijunasdb?useSSL=false&serverTimezone=UTC

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true